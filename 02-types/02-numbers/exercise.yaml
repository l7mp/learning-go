name: calculator
input:
- descr: "`x` over `y` as an percentage rounded to the nearest integer (so the result for `x=1.2` and `y=2.4` would be 50)"
  funcName: "percentage"
  func: "func percentage(x,y float64) int"
  sol0: 2
  sol1: 4
  sol2: 24
  funcstring: "func percentageString(x,y string) (int, error)"
- descr: "arithmetic mean of `x` and `y` rounded to the nearest integer (so the result for `x=1.2` and `y=2.4` would be 2)"
  funcName: "amean"
  func: "func amean(x,y float64) int"
  sol0: 2
  sol1: 52
  sol2: 89
  funcString: "func ameanString(x,y string) (int, error)"
- descr: "geometric mean of `x` and `y` rounded to the nearest integer (so the result for `x=1.2` and `y=2.4` would be `round(sqrt(1.2*2.4)=round(1.69)=2`)"
  funcName: "gmean"
  func: "func gmean(x,y float64) int"
  funcString: "func gmeanString(x,y string) (int, error)"
  sol0: 2
  sol1: 19
  sol2: 71
